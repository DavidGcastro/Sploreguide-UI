// import dotenv from 'dotenv'
// dotenv.config({ silent: true })

export const { GRAPHQL_SERVER } = `http://localhost:3000/graphql`

// const defaults = {
//   GRAPHQL_SERVER: 'DEFAULT_KEY'
// }

// Object.keys(defaults).forEach((key) => {
//   if (!process.env[key] || process.env[key] === defaults[key]) {
//     throw new Error(`Please enter a custom ${key} in .env on the root directory`)
//   }
// })

// export default GRAPHQL_SERVER
